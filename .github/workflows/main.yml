name: Cypress Tests Pipeline

on: [push]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código 🛎️
        uses: actions/checkout@v4

      - name: Instalar dependências e Rodar Cypress 🌲
        uses: cypress-io/github-action@v6
        with:
          browser: chrome